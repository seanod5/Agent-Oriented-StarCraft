agent Tests {

	module Console C;
	module Prelude P;
	
	rule +!main(list args) {
		list a = [1, 2];
		list b = [3, 4];
		list c = [a, b];
		C.println(c);
		list d = [5, 6];
		c = c + [d];
		C.println(c);
	}
}
