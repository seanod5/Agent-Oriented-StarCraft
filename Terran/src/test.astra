agent test {
	module modules.Strings strings;
	module modules.Prelude2 P;
	module Math math;
	module Console C;
	module System S;
	
	types shit {
		formula array(list);
	}
	
	initial array([1]);
	
	rule +!main(list args) {
		list array = [1];
		P.add(array, 2);
		C.println(array);
	}
}
