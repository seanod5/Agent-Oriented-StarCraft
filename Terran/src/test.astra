agent test {
	module modules.SupplyDepot SD;
	module modules.Strings strings;
	module modules.Lists lists;
	module Math math;
	module Console C;
	module Prelude P;
	module System S;
	
	types shit {
		formula hey(int);
		formula ho(boolean);
	}
	
	initial hey(1);
	initial ho(false);
	
	rule +!main(list args) {
		C.println("Start...");
		boolean check = query(hey(2));
		C.println(check);
		if(check == true) {
			C.println("hey");
		}
		S.exit();
	}
}
